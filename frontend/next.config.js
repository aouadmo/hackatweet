/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['cdn.vox-cdn.com', 'platform.theverge.com'],
    },
  };
  
  module.exports = nextConfig;
  